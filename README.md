# Auto Docs

This is a minimal Django REST API project created using `uv` for package management.

## Setup

1.  Ensure you have `uv` installed.
2.  Install dependencies:
    ```bash
    uv sync
    ```

## Running the server

To start the development server:
```bash
uv run python manage.py runserver
```

## API Endpoints

-   `GET /api/hello/` - Returns a "Hello, world!" message.

## Background

Created by Antigravity as a template for a minimal Django REST project.
